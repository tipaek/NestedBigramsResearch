import java.io.*;

public class Latin {
    public static void main(String[] args) {
        int T = 0; // Initialize T to some value
        int N = 10; // Initialize N to some value, for example 10
        for (int i = 1; i <= N; i++) {
            switch (i) {
                case 1:
                    // Add some functionality for case 1
                    System.out.println("Case 1 executed for i = " + i);
                    break;
                // Add more cases if needed
                default:
                    System.out.println("Default case executed for i = " + i);
                    break;
            }
        }
    }
}